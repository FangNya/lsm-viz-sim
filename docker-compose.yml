services:
  backend:
    build:
      context: ./backend
    container_name: lsm_backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: lsm_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend